// this extra chapter is added to make notes about DOM tutorial

// innterHtml is the content inside an HTML tag. there may be content and more tags inside innerHtml. if you change the innerHTML everything will delete so better check what inside the inner html an target only one element that you want to change. you can always check and print what is inside the specific tag. 